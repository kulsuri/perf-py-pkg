"""Core functions for perf_py_pkg."""


def add_one(number: int) -> int:
    """Add one to a number.

    Args:
        number (int): number to add one to

    Returns:
        int: number + one
    """
    return number + 1
